
const FIELDS = foundry.data.fields

/**
 * 
 * @extends {JournalEntry} https://foundryvtt.com/api/classes/client.JournalEntry.html
 */
export default class LoggerJournal extends JournalEntry {
    static defineSchema() {
        return {
            
        }
    }
}