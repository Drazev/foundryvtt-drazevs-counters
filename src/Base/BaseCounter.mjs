/**
 * This is base counter that is intended to define the basic counter functionality that all counters share.
 * 
 * Counters define the behaviors associated with a counter necessary to move the counter from one valid state
 * to another. It also determines when a counter should emit or respond to hook events.
 * 
 * 
 * A base counter should define a single DataModel used to store any data with meaning in the game.
 * The DataModel should NOT include transient data or calculated data.
 * 
 * In gene
 * 
 */

export default class BaseCounter {
   
    
}